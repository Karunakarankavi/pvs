spring.application.name=res_pvs
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://cognito-idp.ap-south-1.amazonaws.com/ap-south-1_oNbMDAFBy
spring.datasource.url=${MYSQL_DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect



razorpay.key_id=${RAZORPAYKEYID}
razorpay.key_secret=${RAZORPAYKEYSECRET}
razorpay.webhook.secret=${RAZORPAYWEBHOOK}
# JPA/Hibernate

server.port=5000

